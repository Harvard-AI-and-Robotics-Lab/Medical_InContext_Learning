name: slake_gemma4_dinov3_cls_top6_final
seed: 3407
data_root: data/vqa
output_root: outputs/final/slake_gemma4_dinov3_cls_top6
output_file: predictions.json
features_root: outputs/features
inference:
  client_backend: vllm
  model: gemma4-31b-slake
  base_url: http://127.0.0.1:18102/v1
  api_key_env: VLLM_API_KEY
  temperature: 1.0
  top_p: null
  max_tokens: 512
  seed: 3407
  response_format: json_object
  enable_thinking: false
  chat_template_kwargs:
    enable_thinking: false
  timeout: 3000
  parallel_requests: 64
  batch_delay: 0
  image_max_side: null
retrieval:
  k: 6
datasets:
- slake
methods:
- rg_icl_global_similarity
manifest_json: data/vqa/slake/manifest_en.json
lang: en
query_split: test
batch_size: 64
k: 6
retrieval_features_dir: outputs/features_dinov3_global_slake/slake/dinov3
retrieval_score_label: DINOv3 global similarity
prompt_suffix: 'Return only one compact JSON object. Include "answer", "confidence" for confidence in the answer, and "evidence" of eight words max. No markdown, no extra text.'
prompt_suffix_methods:
- rg_icl_global_similarity
