name: extract_dinov3_global_slake
seed: 3407
data_root: data/vqa
output_root: outputs
features_root: outputs/features_dinov3_global_slake
encoder:
  name: dinov3
  model_id: facebook/dinov3-vitl16-pretrain-lvd1689m
  device: cuda
  batch_size: 8
  image_size: 512
  normalize: true
  return_spatial: false
retrieval:
  method: global
  k: 6
  similarity_metric: cosine
  exclude_query: true
  exclude_test_set: true
datasets:
- slake
methods:
- rg_icl_global_similarity
manifest_json: data/vqa/slake/manifest_en.json
lang: en
