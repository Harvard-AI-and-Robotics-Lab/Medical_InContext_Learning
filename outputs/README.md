# Outputs

Model predictions, feature caches, checkpoints, and metric JSON files are
written under this directory.

This directory is intentionally ignored by git because full experiment outputs
and feature tensors are large generated artifacts.
